# TermCrew frontend

Svelte 5 + Vite + Tailwind CSS v4 + xterm.js UI for [TermCrew](../README.md).

```bash
bun install
bun run dev    # http://localhost:5173 (proxies API to :3001)
bun run build
bun run check
```
